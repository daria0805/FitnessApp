{% extends 'base.html.twig' %}

{% block title %}Hello UserController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    .example-wrapper .input-wrapper { margin-bottom: 1em; }
    .container-user {
        margin: 50px auto;
        max-width: 800px;
        padding: 20px;
        text-align: center;
            background-color: #fff;
        border-radius: 5px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
</style>

<div class="container-user">
<div class="example-wrapper">
    <h1>Hello {{ app.user.email }}! ✅</h1><br>
    <div class="input-wrapper">
      <label for="calculate-plan-input">Calculate Diet Plan:</label>
      <a href="{{ path('calculate_diet_plan') }}" class="btn btn-primary">Calculate Diet Plan</a><br><br>
    </div>
    <div class="input-wrapper">
      <label for="go-to-plan-input">Go to Your Plan:</label>
      <a href="{{ path('app_meal_plan') }}" class="btn btn-primary">Go to Plan</a>
    </div>
</div>
</div>
{% endblock %}
